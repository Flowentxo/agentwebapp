# =====================================================
# INTEGRATION PROVIDERS - ENVIRONMENT VARIABLES
# =====================================================
# Copy this file to .env.local and fill in your credentials
#
# WICHTIG: Du benötigst nicht alle Provider!
# Konfiguriere nur die, die du tatsächlich nutzen möchtest.
# =====================================================

# =====================================================
# APP CONFIGURATION
# =====================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
INTEGRATION_ENCRYPTION_KEY=your-32-byte-encryption-key-here

# =====================================================
# GOOGLE (Gmail, Calendar, Drive, Contacts, Analytics)
# =====================================================
# Setup: https://console.cloud.google.com/
# 1. Create Project
# 2. Enable APIs: Gmail, Calendar, Drive, People, Analytics
# 3. OAuth Consent Screen: External, Add Scopes
# 4. Credentials > Create OAuth 2.0 Client ID (Web)
# 5. Add Redirect URI: http://localhost:3000/api/oauth/google/callback
# =====================================================
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret

# =====================================================
# MICROSOFT (Outlook, OneDrive, Calendar, Contacts)
# =====================================================
# Setup: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
# 1. App registrations > New registration
# 2. Supported account types: Personal + Organizational
# 3. Redirect URI: http://localhost:3000/api/oauth/microsoft/callback
# 4. Certificates & secrets > New client secret
# 5. API permissions > Add: Mail, Calendars, Files, Contacts (Delegated)
# =====================================================
MICROSOFT_CLIENT_ID=your-azure-app-id
MICROSOFT_CLIENT_SECRET=your-client-secret

# =====================================================
# HUBSPOT (CRM - Contacts, Deals, Companies)
# =====================================================
# Setup: https://app.hubspot.com/
# 1. Settings > Integrations > Private Apps > Create
# 2. Add Scopes: crm.objects.contacts, crm.objects.deals, etc.
# 3. Copy Client ID and Secret
# 4. For OAuth: Go to https://developers.hubspot.com/
# =====================================================
HUBSPOT_CLIENT_ID=your-hubspot-client-id
HUBSPOT_CLIENT_SECRET=your-hubspot-client-secret

# =====================================================
# SALESFORCE (CRM)
# =====================================================
# Setup: https://developer.salesforce.com/
# 1. Setup > Apps > App Manager > New Connected App
# 2. Enable OAuth Settings
# 3. Callback URL: http://localhost:3000/api/oauth/salesforce/callback
# 4. Selected OAuth Scopes: api, refresh_token, offline_access
# =====================================================
SALESFORCE_CLIENT_ID=your-consumer-key
SALESFORCE_CLIENT_SECRET=your-consumer-secret

# =====================================================
# PIPEDRIVE (CRM)
# =====================================================
# Setup: https://developers.pipedrive.com/
# 1. Developer Hub > Create App
# 2. OAuth & Access Scopes: deals:read, persons:read, etc.
# 3. Callback URL: http://localhost:3000/api/oauth/pipedrive/callback
# =====================================================
PIPEDRIVE_CLIENT_ID=your-pipedrive-client-id
PIPEDRIVE_CLIENT_SECRET=your-pipedrive-client-secret

# =====================================================
# SLACK (Messaging & Channels)
# =====================================================
# Setup: https://api.slack.com/apps
# 1. Create New App > From scratch
# 2. OAuth & Permissions > Scopes: channels:read, chat:write, users:read, etc.
# 3. Redirect URLs: http://localhost:3000/api/oauth/slack/callback
# 4. Install to Workspace
# =====================================================
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret

# =====================================================
# NOTION (Pages, Databases, Blocks)
# =====================================================
# Setup: https://www.notion.so/my-integrations
# 1. New integration
# 2. Internal or Public integration
# 3. Capabilities: Read, Update, Insert content
# 4. For OAuth: Submit for review (Public)
# =====================================================
NOTION_CLIENT_ID=your-notion-client-id
NOTION_CLIENT_SECRET=your-notion-oauth-secret

# =====================================================
# DROPBOX (Files & Folders)
# =====================================================
# Setup: https://www.dropbox.com/developers/apps
# 1. Create App > Scoped access > Full Dropbox
# 2. Permissions: files.metadata, files.content, sharing
# 3. OAuth 2 Redirect URIs: http://localhost:3000/api/oauth/dropbox/callback
# =====================================================
DROPBOX_CLIENT_ID=your-dropbox-app-key
DROPBOX_CLIENT_SECRET=your-dropbox-app-secret

# =====================================================
# STRIPE (Payments, Subscriptions, Invoices)
# =====================================================
# Setup: https://dashboard.stripe.com/apikeys
# 1. Developers > API keys
# 2. For OAuth Connect: https://dashboard.stripe.com/settings/connect
# =====================================================
STRIPE_CLIENT_ID=ca_your-stripe-connect-client-id
STRIPE_SECRET_KEY=flwnt_test_your-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key

# =====================================================
# PAYPAL (Payments, Transactions)
# =====================================================
# Setup: https://developer.paypal.com/dashboard/applications
# 1. My Apps & Credentials > Create App
# 2. Copy Client ID and Secret
# =====================================================
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret

# =====================================================
# QUICKBOOKS (Accounting, Invoices)
# =====================================================
# Setup: https://developer.intuit.com/
# 1. Create App > Accounting
# 2. Keys & credentials > OAuth 2.0
# 3. Redirect URI: http://localhost:3000/api/oauth/quickbooks/callback
# =====================================================
QUICKBOOKS_CLIENT_ID=your-quickbooks-client-id
QUICKBOOKS_CLIENT_SECRET=your-quickbooks-client-secret

# =====================================================
# XERO (Accounting)
# =====================================================
# Setup: https://developer.xero.com/
# 1. My Apps > New App
# 2. OAuth 2.0 Redirect URI: http://localhost:3000/api/oauth/xero/callback
# =====================================================
XERO_CLIENT_ID=your-xero-client-id
XERO_CLIENT_SECRET=your-xero-client-secret

# =====================================================
# LINKEDIN (Social - Profile, Posts)
# =====================================================
# Setup: https://www.linkedin.com/developers/apps
# 1. Create App
# 2. Products: Sign In, Share on LinkedIn, Marketing Developer Platform
# 3. Auth: Redirect URL: http://localhost:3000/api/oauth/linkedin/callback
# =====================================================
LINKEDIN_CLIENT_ID=your-linkedin-client-id
LINKEDIN_CLIENT_SECRET=your-linkedin-client-secret

# =====================================================
# TWITTER / X (Tweets, Timeline)
# =====================================================
# Setup: https://developer.twitter.com/en/portal/dashboard
# 1. Projects & Apps > Create App
# 2. User authentication settings > OAuth 2.0
# 3. Callback URL: http://localhost:3000/api/oauth/twitter/callback
# =====================================================
TWITTER_CLIENT_ID=your-twitter-client-id
TWITTER_CLIENT_SECRET=your-twitter-client-secret

# =====================================================
# FACEBOOK / META (Pages, Posts)
# =====================================================
# Setup: https://developers.facebook.com/apps/
# 1. Create App > Business
# 2. Facebook Login > Settings
# 3. Valid OAuth Redirect URIs: http://localhost:3000/api/oauth/facebook/callback
# =====================================================
FACEBOOK_CLIENT_ID=your-facebook-app-id
FACEBOOK_CLIENT_SECRET=your-facebook-app-secret

# =====================================================
# INSTAGRAM (via Facebook/Meta)
# =====================================================
# Uses same Facebook App with Instagram permissions
INSTAGRAM_CLIENT_ID=your-facebook-app-id
INSTAGRAM_CLIENT_SECRET=your-facebook-app-secret

# =====================================================
# TIKTOK (Videos, Profile)
# =====================================================
# Setup: https://developers.tiktok.com/
# 1. Create App > Sandbox/Live
# 2. Login Kit > Add Products
# 3. Redirect URI: http://localhost:3000/api/oauth/tiktok/callback
# =====================================================
TIKTOK_CLIENT_KEY=your-tiktok-client-key
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret

# =====================================================
# STRAVA (Fitness - Activities)
# =====================================================
# Setup: https://www.strava.com/settings/api
# 1. Create App
# 2. Authorization Callback Domain: localhost
# =====================================================
STRAVA_CLIENT_ID=your-strava-client-id
STRAVA_CLIENT_SECRET=your-strava-client-secret

# =====================================================
# FITBIT (Fitness - Health Data)
# =====================================================
# Setup: https://dev.fitbit.com/apps/new
# 1. Register App > Personal
# 2. OAuth 2.0 Application Type: Server
# 3. Callback URL: http://localhost:3000/api/oauth/fitbit/callback
# =====================================================
FITBIT_CLIENT_ID=your-fitbit-client-id
FITBIT_CLIENT_SECRET=your-fitbit-client-secret

# =====================================================
# GARMIN (Fitness - Wellness API)
# =====================================================
# Setup: https://developer.garmin.com/
# 1. Request API Access (requires approval)
# 2. Create App after approval
# =====================================================
GARMIN_CONSUMER_KEY=your-garmin-consumer-key
GARMIN_CONSUMER_SECRET=your-garmin-consumer-secret

# =====================================================
# MIXPANEL (Analytics)
# =====================================================
# Setup: https://mixpanel.com/
# 1. Project Settings > Access Keys
# 2. Service Account for API access
# =====================================================
MIXPANEL_PROJECT_TOKEN=your-project-token
MIXPANEL_API_SECRET=your-api-secret

# =====================================================
# HOTJAR (Analytics - Heatmaps, Recordings)
# =====================================================
# Setup: https://insights.hotjar.com/
# 1. Integrations > API
# 2. Generate Personal Access Token
# =====================================================
HOTJAR_API_KEY=your-hotjar-api-key
HOTJAR_SITE_ID=your-site-id

# =====================================================
# YOUTUBE (via Google)
# =====================================================
# Uses same Google OAuth with YouTube API enabled
# Enable: YouTube Data API v3 in Google Cloud Console

# =====================================================
# ZAPIER (Automation - Webhooks)
# =====================================================
# No OAuth needed - uses API keys per Zap
# ZAPIER_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/...

# =====================================================
# MAKE / INTEGROMAT (Automation)
# =====================================================
# No OAuth needed - uses webhooks
# MAKE_WEBHOOK_URL=https://hook.make.com/...
